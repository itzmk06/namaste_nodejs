console.log("Hello world from app.js");

const calculateSum=(a,b)=>{
    const sum=a+b;
    console.log(`Sum of ${a} and ${b} is ${sum}`);
}

const calculateProduct=(a,b)=>{
    const product=a*b;
    console.log(`Product of ${a} and ${b} is ${product}`);
}

const message="Hello Manoj!";

console.log(`Yes it's an empty object ${module.exports}`);

module.exports={calculateSum,message,calculateProduct}